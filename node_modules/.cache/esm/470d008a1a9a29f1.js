let Discord,Message,Client,roles,Commands;_1a9‍.w("discord.js",[["default",["Discord"],function(v){Discord=v}],["Message",["Message"],function(v){Message=v}],["Client",["Client"],function(v){Client=v}]]);_1a9‍.w("./roles.js",[["default",["roles"],function(v){roles=v}]]);_1a9‍.w("./commands.js",[["default",["Commands"],function(v){Commands=v}]]);/**
 * @name        PantherBot
 * @version     0.6.0
 * @author      GSUGuide21 <https://github.com/GSUGuide21>
 **/
// import express from "express";
// import bodyParser from "body-parser";



// import { MongoClient } from "mongodb";


// import Events from "./events";
// import config from "./config";

// const app = express( );
const bot = new Client( );
const token = "NzU3MDQ2MTU4MTQ4MzcwNDcz.X2asLQ.JPpBHgmK_B9EGovbpcwJaEfgroA";
const parse = ( { content } ) => { 
    const [ command, ...args ] = content.split( /\s+/g );
    const commandName = command.replace( /^(?:\!|\/)/, "" );
    if ( typeof Commands[ commandName ] === "function" ) { 
        return { commandName, args, valid : true };
    } else {
        return { commandName, valid : false }
    }
};

bot.on( "message", msg => { 
    const { author: { id } } = msg;
    if ( id === bot.user.id ) return;
    const { valid, commandName, args } = parse( msg );
    const { channel: { send } } = msg;
    if ( valid ) { 
        const val = Commands[ commandName ]( msg, args );
        if ( val !== void 0 ) send( val );
    } else {
        msg.reply( `The following command is invalid: ${ commandName }.` );
        // Commands.help( );
    }
} );

bot.on( "ready", ( ) => { 
    console.log( "Bot is now connected!" );
} );

bot.login( token );