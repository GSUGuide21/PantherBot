import Discord from "discord.js";

export default { 
	commands : [ "poll", "p" ],
	type : "poll",
	/**
	 * @param {Discord.Message} msg
	 * @param {string[]} args
	 * @param {string} text
	 **/
	async run( msg, args, text ) { 
		
	}
}
